# Ensure AppFrameworkInfo.plist exists (required by Flutter)
app_framework_plist = File.join('ios', 'Flutter', 'AppFrameworkInfo.plist')
unless File.exist?(app_framework_plist)
  File.open(app_framework_plist, 'w') do |file|
    file.write <<~PLIST
      <?xml version="1.0" encoding="UTF-8"?>
      <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
      <plist version="1.0">
      <dict>
        <key>CFBundleShortVersionString</key>
        <string>1.0.0</string>
        <key>CFBundleVersion</key>
        <string>1</string>
        <key>CFBundleIdentifier</key>
        <string>io.flutter.flutter.app</string>
      </dict>
      </plist>
    PLIST
  end
end
